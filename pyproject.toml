[project]
name = "dl-youtube"
version = "0.2.0"
description = "YouTube video downloader with dubbing support"
requires-python = ">=3.10"
dependencies = [
    "yt-dlp>=2024.0.0",
    "pysocks>=1.7.0",
]
